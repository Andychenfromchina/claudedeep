All evidence has been gathered above (search results and fetched pages).
Now write the polished final markdown report following the format spec
in the system prompt. Cite using only the URLs that actually appear in
the tool results above; do not invent sources.
